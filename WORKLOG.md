# 工作记录 / WORKLOG

## 2026-08-28

**涉及提交**: `9c97d12` `63ff35b` `a4ae541` `66b9254`（均已推送 origin/main）

### 一、专项排查：哔咔残留与 MangaBZ 热榜失效（用户报告）

**1. 哔咔（PicAcg）残留**
- 全仓排查（Go 源码 / 前端源码 / 编译产物 dist / 配置）：源码层面已无任何哔咔痕迹，`5c96fa3` 确实删干净了。
- 结论：服务器仍见哔咔是因为 **旧 Docker 镜像未重建（`up -d` 未带 `--build`）或浏览器缓存了旧 index.html**，非代码问题。
- 清理：删除遗留脚本 `cmd/pica_test`；README 增加升级排障说明（强刷 / `--build`）。

**2. 首页 MangaBZ 热榜"未在页面提取到榜单列表"**
- 实测（用户提供代理 `192.168.89.10:20170`）：页面结构完好、现有选择器无误，真正的根因在传输层——
  - 本网络 DNS 将 mangabz 污染至 `199.96.59.61`（Twitter IP 段）等无关地址；
  - 代码的混合传输"先直连后代理"且全局 `InsecureSkipVerify: true`，被污染 IP 返回的 200 垃圾页被当作真实页面解析 → 0 条目。
- 修复（`9c97d12`）：
  - TLS 证书校验默认开启（新增 `skip_tls_verify` 配置项，默认 `false`）——污染 IP 拿不出合法证书，直连立即失败落到代理；
  - 抓取页按内容标记校验，不匹配则标记该域名"仅代理"并自动走代理重试（`fetchPageForScraping`）；
  - 榜单支持多候选 URL 依次回退；错误信息附带诊断数据（字节数/标记数/页面标题）。
- 验证：真实链路三源全部拉通（MangaBZ 12 条 / DM5 7 条 / CopyManga 12 条），搜索"鬼滅之刃"相关性排序正确。

### 二、代码质量与安全修复（全量审查发现的 bug）

- **Task 数据竞争**：SSE/JSON 读取现在拿锁内快照（`Task.clone()` 深拷贝），worker 全部写操作统一在 `m.mu` 内；`processTask` 原子认领任务，双击恢复不再可能双 worker 并发处理同一任务。
- **恢复任务重复计数**：断点续传恢复后进度从 0 重新统计，不再出现进度 >100%。
- **静默丢页**：DM5 / MangaBZ 任何一页拉取失败现在整体报错并触发自动换源；下载阶段缺任何一张图任务即失败——不再产出"100% 完成"但缺页的坏文件。
- **`check_interval_minutes` 之前完全无效**（调度器硬编码 30 分钟）：改为每分钟心跳对比配置间隔，改配置即时生效。
- **安全**：移除 `Access-Control-Allow-Origin: *`（任意网页此前可跨域读本 API）；TLS 校验见上。
- **性能**：每个任务复用一个 HTTP 客户端（原每张图新建 Transport，全量重新 TLS 握手）；PDF 打包对 JPEG 原图直通不再有损重编码。

### 三、部署与配置调整

- **docker-compose（`63ff35b` / `a4ae541`）**：
  - 删除全部代理环境变量（`HTTPS_PROXY→HTTP_PROXY→ALL_PROXY` 只读第一个非空值，冗余；config.json 优先级更高，代理统一走网页配置）；
  - 三个状态文件改由宿主机 `./config/` 挂载（`config/config.json` 等），README 的 `touch` 前置步骤补上 `config.json`；
  - 新增注释状态的 SMB/NAS 下载目录挂载（宿主机 fstab 方式 / compose CIFS 卷方式二选一）。
- **代理即改即生效（`a4ae541`）**：`POST /api/config` 保存时主动清空首页榜单缓存并重置直连/代理路由表——网页里改代理无需重启容器（此前受 5 分钟缓存与进程级路由记忆影响）。
- **仓库卫生（`9c97d12`）**：修复 `.gitignore`（`/dist/` 锚定，`web/dist/.gitkeep` 可被跟踪），`go:embed all:web/dist` 保证 fresh clone 可编译；README 移除已删除的集英社描述、补充 `skip_tls_verify` 与部署注意事项。

### 四、搜索/详情修复（用户报告）

- **DM5 标题解析成"登录"（`66b9254`）**：DM5 详情页唯一的 `<h1>` 是登录弹窗（海外 IP 策略），旧选择器未命中真实标题 `p.title`，兜底 `h1` 抓到"登录"，导致"无章节面板 + 跨源检索《登录》"。现按优先级选择器解析 + 垃圾标题过滤（`pickTitle`）+ `<title>` 标签兜底 + 剥离评分子元素；MangaBZ 同样接入防护。
- **前端（`66b9254`）**：详情弹窗展示后端错误（此前白屏）；"无章节"面板与跨源检索按钮改用搜索结果原始标题，不再依赖解析标题。
- **拷贝漫画（`66b9254`）**：清理已死镜像（`copymanga.tv/.org/.site`、`copy-manga.com` web，每次取详情白等 15s 超时）；两组策略均拿不到章节时返回明确错误（区域隐藏/站点维护提示）而非静默的 0 章节假详情。

### 五、遗留问题与后续

1. **拷贝漫画章节目录当前大面积为空**（服务端问题，非源地址错误）：API 返回 200+空列表、web 加密接口解密后 `count=0`，伴随"服務器升級中"页面；且间歇性恢复过（实测某时刻返回 39 章节）。疑似按出口 IP 区域隐藏内容，建议**更换代理出口节点**再试；代码侧已有明确报错提示。
2. 首页热榜为实时人气周榜（非硬编码），与官网展示不同是榜单类型/时间窗口差异；如需与官网一致可调整 rank type 参数。
3. 服务器验证待做：`git pull` → 旧 json 迁移至 `./config/` → `docker compose up -d --build` → 浏览器强刷。
4. PDF 打包仍基于已停止维护的 `jung-kurt/gofpdf`，长期建议迁移（如 gopdf/pdfcpu）。
